# Synthetic data — derived from the agent's ingress (XI) contract

These 200 sample events are shaped by **this agent's own ingress port
contract** (fields: event_id, ts, source, payload, severity), not by any vertical/profile
template. Values are neutral and typed — replace them with real domain data
for your agent, or let an SLM/LLM seed realistic values against the same schema.

Consumed by `demo_runner.py` and rate-limited there.
