# Auto-generated by Forseti bundleBuilder (Fas 2.14a — supply-chain pinning).
# The GitHub Actions below run on pinned commit SHAs, the base image is pinned by
# digest (Fas 2.14b), and the Python deps are locked. Dependabot opens PRs to bump
# each pin when a newer version ships — review + merge to update deliberately.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10
    commit-message:
      prefix: "ci(deps)"
  - package-ecosystem: "docker"
    directory: "/deploy"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10
    commit-message:
      prefix: "build(deps)"
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10
    commit-message:
      prefix: "deps"
