def test_orchestrator_loads_karta():
    """CUSTOMER_SLOT: orchestrator unit coverage.

    Generated scaffold — asserts trivially until customer logic exists.
    Replace with real assertions against src.shared.orchestrator once the
    handlers are implemented (see README section 'Binding an LLM provider').
    """
    assert True
